import { AuthWidget } from "src/modules/auth";
import { Box } from "src/shared/ui";

const AuthPage = () => {
    return (
        <Box>
            <AuthWidget/>
        </Box>
    );
}
export default AuthPage;
